import Link from 'next/link';
import { styled } from '@mui/material/styles';

const FooterControlLink = styled(Link)(() => ({
  display: 'inline-block',
  flex: '1 0 50%',
}));

export default FooterControlLink;
